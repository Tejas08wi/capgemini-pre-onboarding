SELECT CITY, LENGTH(CITY)
FROM (
    SELECT CITY, LENGTH(CITY) AS LEN
    FROM STATION
    ORDER BY LEN, CITY
)
WHERE ROWNUM = 1
UNION ALL
SELECT CITY, LENGTH(CITY)
FROM (
    SELECT CITY, LENGTH(CITY) AS LEN
    FROM STATION
    ORDER BY LEN DESC, CITY
)
WHERE ROWNUM = 1;
